### How to run the project
1. `cd hand_gesture/`
2. Start the Python server with `python run main.py`
3. `cd ../frontend`
4. `yarn install` to install the node packages
5. `yarn run start` to start electron
